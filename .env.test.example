# Test Database Configuration
# Copy this file and fill in your test database credentials

# Test database name (will be created if not exists)
DB_TEST_NAME=mnx_agent_test

# Test database user (optional, defaults to DB_USER)
DB_TEST_USER=mnx_agent_server_test

# Test database password (optional, defaults to DB_PASSWORD)
DB_TEST_PASSWORD=test_password_123